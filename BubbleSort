def bubbleSort(array):
    # Write your code here.
	i=0
	j=0
	temp = 0
	length = len(array) 
	for i in range(0,length-1):
		for j in range(i+1,length):
			if array[i] > array[j]:
				temp = array[i]
				array[i] = array[j]
				array[j] = temp
	return array
    pass
